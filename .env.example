# AgentMesh — example environment variables
# Copy to .env and fill in your values.

# Gateway bootstrap password (required on first boot if config.json is empty)
AGENTMESH_BOOTSTRAP_PASSWORD=change-me-strong-password
