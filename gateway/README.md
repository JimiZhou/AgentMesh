# AgentMesh Gateway

Minimal Gateway prototype for AgentMesh.

- HTTP API on /api/*
- WebSocket runner channel on /ws/runner
- JSON state persisted under gateway/data/state.json

Run:

1) cd gateway
2) npm i
3) npm run dev

Then open:
- http://127.0.0.1:8787/health
